<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 更完善的ContextMenu样式 -->
    <Style x:Key="ModernContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Background" Value="CadetBlue" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="#12dabd" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="1" />

        <!-- 阴影效果 -->
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="8"
                    Direction="270"
                    Opacity="0.6"
                    ShadowDepth="2"
                    Color="Black" />
            </Setter.Value>
        </Setter>

        <!-- 控制模板 -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                        <ScrollViewer x:Name="ScrollViewer" CanContentScroll="True" Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                            <ItemsPresenter
                                x:Name="ItemsPresenter"
                                Grid.IsSharedSizeScope="True"
                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                KeyboardNavigation.TabNavigation="Cycle"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasDropShadow" Value="True">
                            <Setter TargetName="Border" Property="Padding" Value="0,3,0,3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!-- MenuItem样式 -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="MenuItem">
                    <Setter Property="Height" Value="28" />
                    <Setter Property="Padding" Value="12,0,12,0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="FontFamily" Value="Consolas" />
                    <Setter Property="FontSize" Value="16" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="MenuItem">
                                <Border
                                    x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="2"
                                    SnapsToDevicePixels="True">
                                    <Grid x:Name="Grid" Margin="2,0,2,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" MinWidth="18" SharedSizeGroup="Icon" />
                                            <ColumnDefinition Width="*" SharedSizeGroup="Text" />
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut" />
                                            <ColumnDefinition Width="Auto" MinWidth="10" SharedSizeGroup="Arrow" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Icon -->
                                        <ContentPresenter
                                            x:Name="Icon"
                                            Width="16"
                                            Height="16"
                                            Margin="0,0,6,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Icon}"
                                            ContentSource="Icon" />

                                        <!-- Header -->
                                        <ContentPresenter
                                            x:Name="Header"
                                            Grid.Column="1"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Header}"
                                            ContentSource="Header"
                                            RecognizesAccessKey="True" />

                                        <!-- Shortcut Text -->
                                        <TextBlock
                                            x:Name="InputGestureText"
                                            Grid.Column="2"
                                            Margin="20,0,6,0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            Foreground="Black"
                                            Text="{TemplateBinding InputGestureText}" />

                                        <!-- Arrow for submenu -->
                                        <Path
                                            x:Name="Arrow"
                                            Grid.Column="3"
                                            Margin="10,0,10,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Data="M 0,0 L 4,4 L 0,8 Z"
                                            Fill="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                            Stretch="Uniform"
                                            Visibility="Collapsed" />
                                    </Grid>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <!-- 鼠标悬停效果 -->
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="LightGray" />
                                        <Setter TargetName="Header" Property="TextElement.Foreground" Value="Black" />
                                    </Trigger>

                                    <!-- 禁用状态 -->
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="Header" Property="Opacity" Value="0.5" />
                                        <Setter TargetName="Icon" Property="Opacity" Value="0.5" />
                                        <Setter TargetName="InputGestureText" Property="Opacity" Value="0.5" />
                                        <Setter TargetName="Arrow" Property="Opacity" Value="0.5" />
                                    </Trigger>

                                    <!-- 子菜单箭头显示 -->
                                    <Trigger Property="HasItems" Value="True">
                                        <Setter TargetName="Arrow" Property="Visibility" Value="Visible" />
                                    </Trigger>

                                    <!-- 图标显示逻辑 -->
                                    <Trigger Property="Icon" Value="{x:Null}">
                                        <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>

        <!-- 分隔线样式 -->
        <Style.Resources>
            <Style TargetType="Separator">
                <Setter Property="Height" Value="1" />
                <Setter Property="Margin" Value="8,4,8,4" />
                <Setter Property="Background" Value="#404040" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Separator">
                            <Border Height="{TemplateBinding Height}" Margin="{TemplateBinding Margin}" Background="{TemplateBinding Background}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Style.Resources>
    </Style>

    <!-- 亮色主题样式 -->
    <Style x:Key="LightModernContextMenuStyle" BasedOn="{StaticResource ModernContextMenuStyle}" TargetType="ContextMenu">
        <Setter Property="Background" Value="#FFFFFF" />
        <Setter Property="Foreground" Value="#212121" />
        <Setter Property="BorderBrush" Value="#E0E0E0" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="6"
                    Direction="270"
                    Opacity="0.2"
                    ShadowDepth="1"
                    Color="Black" />
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>