<UserControl
    x:Class="ImageHandle.UserControls.DstImgShowUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ImageHandle.UserControls"
    xmlns:converter="clr-namespace:ImageHandle.Converters"
    Width="400"
    Height="410"
    d:DesignHeight="400"
    d:DesignWidth="410"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:MatToImageSourceConverter x:Key="matToImageSourceConverter" />
    </UserControl.Resources>
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <!--#region 第一行-->
        <TextBlock
            Grid.Row="0"
            Margin="0,2,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Text="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontSize="20"
            Foreground="White" />
        <Button
            Grid.Row="0"
            Width="35"
            Height="25"
            Margin="0,0,2,2"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Content="清空"
            Click="Button_Click" />
        <!--#endregion-->

        <Image
            Grid.Row="1"
            Width="335"
            Height="335"
            Source="{Binding DstImageSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource matToImageSourceConverter}}"
            Stretch="Fill"
            MouseLeftButtonDown="Image_MouseLeftButtonDown" />
        <StackPanel
            Grid.Row="2"
            Margin="0,0,0,1"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <Label
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="图像路径:"
                Foreground="White"
                FontSize="16"
                Background="Transparent" />
            <TextBox
                Width="280"
                Margin="0,2,0,0"
                HorizontalAlignment="Center"
                Text="{Binding ImageSource_Save, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsReadOnly="True"
                Background="Transparent"
                FontSize="18"
                Foreground="White"
                TextAlignment="Center"
                ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
            <Button
                Width="35"
                Height="25"
                Margin="2,0,0,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Content="保存"
                Click="Button_Click_Save" />
        </StackPanel>
    </Grid>
</UserControl>